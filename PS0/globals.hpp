#ifndef _GLOBALS_H
#define _GLOBALS_H

#include <cstdlib>

// float random in [0, 1)
#define RNG ((float) std::rand() / (float) RAND_MAX)

const static int WIDTH = 960;
const static int HEIGHT = 800;

#endif